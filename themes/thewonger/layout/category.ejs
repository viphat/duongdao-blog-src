<div class="all-posts-body">
  <h1>All "<%= page.category %>" Posts</h1>
  <% var posts = page.posts.sort('-date').toArray(); %>
  <% posts.forEach(function(post) { %>
    <div class="article">
      <a href="<%= post.permalink %>"><%= post.title %></a>
      <sub><%= date(post.date, 'YYYY-MM-DD') %></sub>
    </div>
  <% }); %>
</div>